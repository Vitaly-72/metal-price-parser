name: Metal Products Parser

on:
  workflow_dispatch:  # Ручной запуск
  schedule:
    - cron: '0 8 * * *'  # Ежедневно в 8:00

jobs:
  parse:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Даем разрешение на запись для коммитов
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Полная история коммитов
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run parser
      env:
        TRIMET_ARMATURA_URL: ${{ secrets.TRIMET_ARMATURA_URL }}
        PARAD_ARMATURA_URL: ${{ secrets.PARAD_ARMATURA_URL }}
      run: |
        python main.py
        
    - name: Upload results as artifact
      uses: actions/upload-artifact@v4
      with:
        name: parsing-results
        path: results.json
        
    - name: Commit and push results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # Проверяем есть ли изменения
        if git diff --quiet --exit-code results.json; then
          echo "No changes to commit"
        else
          git add results.json
          git commit -m "🤖 Auto-update: parsing results [$(date +'%Y-%m-%d %H:%M')]"
          git push
        fi
